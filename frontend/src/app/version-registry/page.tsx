import { VersionRegistryManager } from '@/components/version-registry-manager'

export default function VersionRegistryPage() {
  return (
    <div className="min-h-screen bg-gray-50">
      <VersionRegistryManager />
    </div>
  )
}
